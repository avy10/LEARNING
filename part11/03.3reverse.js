// REVERSE
// as the name suggests, reverse method reverses the elements of array
// reverse method also mutates the original array

const arr = ["abc", "xyz", "mno", "qrs"];
arr.reverse();

console.log(arr);